---
description: Apply Terraform config (infra changes)
---

// turbo-all

1. From the server directory, run:

```bash
cd /Users/antoniaantonova/Documents/projects/dots/server && lets apply-terraform
```

2. Confirm the output shows the Cloud Run service URL.
