---
description: Build and push the Lumen server Docker image via Cloud Build
---

// turbo-all

1. From the server directory, run:

```bash
cd /Users/antoniaantonova/Documents/projects/dots/server && lets build
```

2. Wait for the build to complete. It should take ~1-2 min if only app code changed, or ~15-20 min if dependencies changed.

3. Confirm the output shows "SUCCESS".
