---
description: Deploy latest image to Cloud Run
---

// turbo-all

1. From the server directory, run:

```bash
cd /Users/antoniaantonova/Documents/projects/dots/server && lets deploy
```

2. Confirm the Cloud Run service URL in the output.
