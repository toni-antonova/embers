---
description: Full release — build, deploy, and health check
---

// turbo-all

1. From the server directory, run:

```bash
cd /Users/antoniaantonova/Documents/projects/dots/server && lets release
```

2. Wait for build + deploy to complete. Verify the health check output at the end.
