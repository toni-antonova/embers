# Cloud Build ignore — minimize upload tarball
# Only the server/ directory is needed for Docker builds.

# ── Git ──────────────────────────────────────────────────────────────
.git/
.gitignore

# ── Frontend (not needed for server Docker build) ────────────────────
node_modules/
dist/
src/
public/
index.html
vite.config.ts
vitest.setup.ts
tsconfig*.json
eslint.config.js
package.json
package-lock.json

# ── Documentation ────────────────────────────────────────────────────
planning/
analysis/
*.md
!README.md
!infrastructure/README.md

# ── Spreadsheets / data files ────────────────────────────────────────
*.xlsx
*.csv
*.xls
data/

# ── Agent / editor config ────────────────────────────────────────────
.agent/
.gemini/
.idea/
.vscode/
*.swp

# ── OS artifacts ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Python dev artifacts ─────────────────────────────────────────────
.venv/
server/.venv/
*.egg-info/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/

# ── Terraform (not needed for Docker build) ──────────────────────────
infrastructure/terraform/*.tfstate
infrastructure/terraform/*.tfstate.backup
infrastructure/terraform/*.tfplan
infrastructure/terraform/.terraform/

# ── Media / large assets ─────────────────────────────────────────────
*.png
*.jpg
*.jpeg
*.gif
*.mp4
*.webm
*.webp
*.wav
*.mp3

# ── Env files (secrets) ─────────────────────────────────────────────
.env
.env.local
.env.production
